# bundle patch for the turn-nav plugin: inserts its plugin row into the web
# profile roster. Applied as a profile bundle layer (the `dsh.bundle.patch`
# manifest field); install with
# `dsh plugin --profile web add link:<repo>/packages/turn-nav`.
- insert:
    - id: ui-turn-nav
      name: '@linxin666/dsh-client-ui-turn-nav'
